# 검증 결과

실행 환경: Node.js 22.16.0, TypeScript 5.8.3, Chromium 144.0.7559.96. 프로젝트에 고정한 npm 의존성 버전과 검사 환경의 전역 TypeScript 버전은 구분한다.

## 실제 실행한 검사

| 구분 | 결과 | 실행 범위 |
|---|---|---|
| 오프라인 빌드 | 통과 | 12개 TS/TSX 모듈 변환, 정적 배포 파일과 단일 HTML 생성 |
| 번들 구문 검사 | 통과 | `node --check dist/assets/app.js` |
| 핵심 모듈 타입 검사 | 통과 | 그래픽 엔진·모델·수학·장 데이터·저장 모듈, strict 및 미사용 검사 |
| 브라우저 시나리오 | 5개 통과 | Chromium, 1280×800 및 375×667 |
| 추가 브라우저 조작 | 통과 | 실제 마우스 드래그, 사용자가 켠 자동 항해의 1→2장 이동과 정지 |
| 데이터·저장·수학 검사 | 6개 통과 | 장·장소 일관성, 정점 유효성, 투영, 메모리 어댑터 저장·복구와 예외 처리 |
| 브라우저 오류 | 0건 | 기본 5개 시나리오의 pageerror 및 콘솔 오류 수집 |

## 브라우저 시나리오 5개

1. 15개 장 순회, 패널·타임라인·렌더링 장 번호 확인, 각 장의 탐색 지점과 선박 확인, 첫·마지막 이동 경계 확인.
2. 지도 지명과 SVG 탐색 지점 선택, 확대·회전 후 실제 투영 결과 변경, 전체 지도·추적 복원, 해협 슬라이더의 배 위치와 장면 변경, 난파·뗏목 전환, 이타카 두 장의 동일 해안 위치, 두 보조 서사와 출처 대화상자.
3. 초기화 취소와 확인, 저장 접근 거부 상황의 안내·정상 탐색, 운영체제 모션 축소, 축소 상태의 정지된 도형, 방향키 이동.
4. 375×667에서 터치 시작·하단 시트·탐색 설명·장 목록, Chromium의 실제 두 포인터 터치 이벤트를 통한 핀치 확대, 가로 넘침 여부.
5. 진행 중 연속 장 선택의 마지막 상태 유지, 일시정지 시 선박과 사건 도형 고정, 재생 후 변화, 자동 항해 기본 비활성, 품질 및 수동 모션 설정.

위 결과는 `artifacts/validation.json`, 추가 조작은 `artifacts/extra-validation.json`, 단위 검사는 `artifacts/unit-validation.json`에 기록했다. `artifacts`의 화면은 실제 렌더링 캡처다.

## 실행 방법과 검증 한계

생성 환경의 브라우저 탐색 정책으로 외부 URL, localhost, file URL 접근이 차단되었다. 따라서 실제 전달하는 `dist/ODYSSEY.html`의 전체 내용을 Playwright `page.set_content`로 Chromium에 넣고 조작했다. 그림만 캡처한 정적 목업 테스트가 아니다.

이 문서 주입 환경에서는 localStorage 접근이 거부된다. 브라우저 테스트는 **접근 거부 폴백**을 검증했고, 정상 저장·복구와 손상 값 검증은 **메모리 저장 어댑터**를 사용하는 단위 테스트로 수행했다. 실제 브라우저 원점에서 새로고침·종료 후 기록이 유지되는지 검증한 결과로 해석하지 않는다.

외부 패키지 다운로드가 차단되어 React 타입 패키지까지 포함하는 전체 타입 검사와 정식 Vite 빌드는 실행하지 못했다. 오프라인 TS/TSX 변환 성공과 핵심 모듈의 의미 기반 타입 검사 성공을 구분한다.

추가 검증 대상: 파일 직접 열기, 정식 npm/Vite 빌드, 공개 서버 배포, Safari·Firefox, 실제 모바일 하드웨어, 숨김 탭 전환·복귀, 실제 브라우저 저장 지속성, 지속 60fps 및 장시간 성능. SVG 평균 깊이 정렬은 복잡한 교차면의 픽셀 단위 정확성을 보장하지 않는다.

## 재실행

```bash
npm run build:offline
npm run check:core
npm run test:unit
python scripts/validate.py
python scripts/extra-browser-check.py
```

브라우저 검사에는 Python Playwright와 Chromium이 필요하다. 기본 실행 파일 경로는 `/usr/bin/chromium`이며, 다른 위치에서는 `CHROMIUM_PATH` 환경변수로 지정한다. 생성 환경의 실제 검사 결과와 일반 환경에서 추가 실행할 수동 인수 기준 `tests/tests.txt`는 구분한다.
